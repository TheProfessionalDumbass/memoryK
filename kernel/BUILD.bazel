load("//build/kernel/kleaf:kernel.bzl", "kernel_module")

kernel_module(
    name = "miprotect",
    srcs = glob([
        "**/*.c",
        "**/*.h",
        "Kbuild",
    ]),
    outs = ["miprotect.ko"],
    kernel_build = "//common:kernel_aarch64",
)

